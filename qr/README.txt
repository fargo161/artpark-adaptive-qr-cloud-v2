FINAL QR IMAGES ARE NOT INCLUDED ON PURPOSE.

After the permanent HTTPS domain is deployed, run:
  npm run qr -- --base-url=https://YOUR-REAL-DOMAIN

Then test every generated code from a real phone before printing.
